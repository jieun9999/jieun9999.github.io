# og-card — SNS 공유 카드

사이트 안의 페이지가 아니라, **링크드인·카카오톡 등에 링크를 붙였을 때 뜨는 이미지**다.
사람들이 글보다 먼저 보는 화면이라 페이지처럼 관리한다. 운영 규칙은 CLAUDE.md "SNS 공유 카드"가 원본이다.

## 한눈에

| | |
| --- | --- |
| 결과물 | 글별 `public/og/<lang>/<slug>.jpg`, 기본 배너 `public/og-default-v4.jpg` |
| 크기 | 1200 × 630 (2배로 그린 뒤 줄여서 글자를 또렷하게) |
| 템플릿 | `scripts/og-card.html` |
| 생성기 | `scripts/gen-og.mjs` (`npm run og`, 템플릿을 고쳤으면 `npm run og -- --force`) |
| 렌더링 | 로컬 크롬 headless — CI 에선 안 돈다. 결과 JPG 를 커밋한다 |
| 쓰는 곳 | 글 상세의 `og:image`. 파일이 없으면 기본 배너로 폴백 (`BaseHead.astro`) |

## 이 화면이 하는 일

링크 미리보기 한 장으로 **무슨 글인지, 누구 블로그인지**를 알린다. 제목이 잘리지 않고 다 보이는 게 가장 중요하다.

## 화면 구조

### 글 카드

```plaintext
1200 × 630
┌──────────────────────────────────────────────────────────────┐
│ ·   .    ·      .        ·   (점 110개, 제목으로 시드 고정)   │
│  ┌ 흰 카드 (사방 44px, 반경 32, --border 선) ──────────────┐  │
│  │ 🌳 jieun.dev                           [RELIABILITY]   │  │ ← 로고 52 + 28/800, 초록 밑줄 3px
│  │    ‾‾‾‾‾‾‾‾‾                                           │  │   카테고리 칩 18/800 대문자
│  │                                                        │  │
│  │ (메신저 부하 테스트 2편)          ← 시리즈 알약 22/800  │  │   제목의 [ ] 안 글자가 알약으로
│  │ AI가 생성한 코드에 deadlock이 숨어 있었다 —             │  │ ← 제목 62px에서 시작,
│  │ unreadCount를 lastReadAt으로 교체한 이야기              │  │   들어갈 때까지 2px씩 줄임(최소 30)
│  │                                                        │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │ 🙂 Jieun · Deserve what you want.             2026.05  │  │ ← 22px, 날짜는 연.월 (고정폭 숫자)
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 기본 배너 (글이 아닌 페이지)

```plaintext
┌──────────────────────────────────────────────┐
│  ·      .       ( 아바타 132 )     ·    .    │
│                    Jieun            76/800   │
│            Deserve what you want.   30/800   │
│        ( jieun.dev · Engineering Blog )      │ ← 초록 알약
└──────────────────────────────────────────────┘
```

## 구성 요소

| 요소 | 모양 |
| --- | --- |
| 바탕 | 사이트 히어로와 같은 `--bg → --bg-2` 세로 그라데이션 + 점. 점 배치는 **제목에서 뽑은 시드**로 정해 다시 만들어도 같은 그림이 나온다 |
| 카드 | 사방 44px 안쪽, 반경 32px, `--border` 1px, 흰 면, 안쪽 52/60/44 |
| 로고 | 앱 아이콘(`web-app-manifest-512x512.png`)을 잘라 나무만 보이게 52px |
| 도메인 | "jieun.dev" 28/800 + **초록 밑줄 3px** (헤더 현재 메뉴와 같은 표시) |
| 카테고리 | `--surface` 칩, 18/800 대문자, 자간 0.08em |
| 시리즈 | 제목 앞 `[시리즈 N편]` 대괄호를 떼어 `--accent-soft` 면 + 초록 글자 알약으로 |
| 제목 | 800, 줄간격 1.24, 자간 -0.015em, **어절 단위 줄바꿈**. 62px 에서 시작해 남은 세로에 들어갈 때까지 줄인다 (자르지 않는다) |
| 아래 줄 | 위 `--border` 선, 아바타 38px + "**Jieun** · Deserve what you want." 22px, 오른쪽 발행 **연.월**(`2026.05`) 700 |
| 글꼴 | Nunito + Pretendard — 사이트와 같은 조합 (woff2 를 임시 폴더로 복사해 쓴다) |
| 아바타 | `public/avatar-illustration.jpg` (홈과 같은 그림) |

## 다크 모드 · 한/영 차이

- **다크 판은 없다.** 라이트 팔레트 한 벌만 쓴다 (미리보기 화면의 테마를 알 수 없어서).
- 언어별로 따로 만든다. `/ko/` 글을 공유하면 한글 제목 카드가 뜬다.

## 규칙

- 🔴 **새 글을 쓰면 `npm run og` 를 돌리고 결과 JPG 를 같이 커밋한다.** 안 하면 링크드인에 제목 없는 기본 배너가 뜬다.
- 🔴 **배너 디자인을 바꾸면 파일명 버전을 올린다** (`og-default-v5.jpg` …) 그리고 `BaseHead.astro` 기본값도 바꾼다. 링크드인·카톡이 이미지를 URL 단위로 캐시한다.
- 🔴 예전 배너(`og-default-v2.jpg`·`v3.jpg`)와 `public/og/*.jpg`(언어 폴더 밖 8장)는 **지우지 않는다.** 이미 공유된 링크가 참조한다.
- 🔴 템플릿 색은 `global.css` 라이트 팔레트와 **같은 값**이어야 한다. 사이트 색을 바꾸면 템플릿도 맞추고 `--force` 로 다시 만든다.
- 제목은 줄여서라도 **전부** 보여준다. `line-clamp` 로 자르지 않는다.

## 알아둘 것

- 🟡 **점 색이 사이트와 다르다.** 사이트 히어로 점은 에버그린 무작위(`fa8651f`)로 바뀌었지만, 카드의 점은 아직 `#18181b` 회색이다. 맞추려면 템플릿의 점 색을 바꾸고 `npm run og -- --force`, 배너는 버전을 `v5` 로 올려야 한다.

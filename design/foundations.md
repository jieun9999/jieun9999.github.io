# foundations — 토큰과 기본 규칙

모든 페이지의 바탕. 값의 원본은 `src/styles/global.css` 의 `:root` / `html.dark` 다.

## 톤 한 줄

**흰 지면 + 징크(zinc) 회색 단계 + 상록(에버그린) 초록 포인트 하나.**
면을 그림자로 띄우지 않고 **얇은 선(`--border`)과 여백**으로 나눈다. 콘텐츠 폭은 1200px.

## 색 토큰

다크 모드는 `html.dark` 클래스로 켠다(미디어쿼리 아님, 아래 "테마" 참고).

### 면

| 토큰 | 라이트 | 다크 | 쓰임 |
| --- | --- | --- | --- |
| `--bg` | `#ffffff` | `#040405` | 페이지 바탕, 카드 바탕 |
| `--bg-2` | `#fafafa` | `#09090b` | 히어로 그라데이션 끝, 인용·시리즈 박스·다음 글 카드 면 |
| `--surface` | `#f4f4f5` | `#18181b` | 칩·태그·버튼 면, hover 면 |
| `--surface-2` | `#e4e4e7` | `#27272a` | 칩 hover, 아이콘 버튼 면 |
| `--card` | `#ffffff` | `#09090b` | (예비) 카드 면 |
| `--code-bg` | `#f4f4f5` | `#18181b` | 인라인 코드 |

### 글자 (진한 순)

| 토큰 | 라이트 | 다크 | 쓰임 |
| --- | --- | --- | --- |
| `--text` | `#18181b` | `#f4f4f5` | 제목, 강조 |
| `--text-2` | `#27272a` | `#e4e4e7` | 본문 |
| `--text-muted` | `#3f3f46` | `#d4d4d8` | 요약·메타·라벨·칩 글자 |
| `--text-subtle` | `#71717a` | `#a1a1aa` | 캡션, 보조 설명, 소개 문단 |
| `--text-faint` | `#a1a1aa` | `#71717a` | 개수 숫자, 목록 마커, 흐린 아이콘 |

### 선

| 토큰 | 라이트 | 다크 | 쓰임 |
| --- | --- | --- | --- |
| `--border` | `#e4e4e7` | `#27272a` | 면 나누는 선, 카드 테두리 |
| `--border-strong` | `#d4d4d8` | `#3f3f46` | 조금 더 보여야 하는 선 (메타의 기울어진 구분선) |
| `--hairline` | `rgba(0,0,0,.06)` | `rgba(255,255,255,.08)` | 이미지·아바타 테두리 |

### 포인트 (에버그린)

라이트·다크를 OKLCH 같은 색상각(h160)에서 뽑아 **두 테마에서 같은 초록으로 느껴지게** 했다.

| 토큰 | 라이트 | 다크 | 쓰임 |
| --- | --- | --- | --- |
| `--accent` | `#307252` | `#72be96` | 글자·링크·현재 메뉴 밑줄 (흰 바탕 5.75:1, 검은 바탕 9.29:1) |
| `--accent-bg` | `#307252` | `#72be96` | 면 — 선택된 칩, 읽기 진행바 |
| `--accent-hover` | `#195e3f` | `#8fd4ad` | 링크 hover |
| `--accent-fg` | `#ffffff` | `#040405` | 초록 면 위 글자 (다크는 밝은 초록 면에 검은 글자로 뒤집힌다) |
| `--accent-soft` | `rgba(48,114,82,.18)` | `rgba(114,190,150,.22)` | 본문 링크 밑줄 |
| `--ring` | `rgba(48,114,82,.45)` | `rgba(114,190,150,.5)` | 키보드 초점 링 |

### 에버그린 단계표

디자인 기준표. 코드에서 쓰는 곳을 같이 적는다.

| 용도 | 1 | 2 | 3 | 4 | 5 | 6 | 코드에서 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 라이트 지면용 | `#195e3f` (hover) | `#256648` | `#307252` (accent) | `#3d8160` | `#4b916e` | `#a9e0c1` (잎맥선) | 히어로 점 `--dot-1..6`, 404 구슬 심(5) |
| 다크 지면용 | `#3e8562` | `#4f9773` | `#5da781` | `#72be96` (accent) | `#8fd4ad` (hover) | `#bcebd1` (잎맥선) | 히어로 점 `--dot-1..6` (다크), 404 구슬 심(6)·몸통(2) |
| 에버그린 타일 위 | `#93d6b1` | `#a9e0c1` | `#bcebd1` | `#cceeda` | `#e2f6ea` | `#ffffff` (잎맥선) | 404 구슬 몸통 (라이트: 5 → 3 → 1) |

다크 404 구슬 몸통의 나머지 두 색 `#256648`·`#195e3f` 는 라이트 지면용 2·1 을 가져다 썼다.

### 그림자

그림자는 **떠 있는 것에만** 쓴다: 검색창(`--shadow-md`), 맨 위로 버튼(`--shadow-md`).
카드·칩·이미지에는 쓰지 않는다.

| 토큰 | 라이트 | 다크 |
| --- | --- | --- |
| `--shadow-sm` | `0 1px 2px rgba(24,24,27,.05)` | `0 1px 2px rgba(0,0,0,.4)` |
| `--shadow-md` | `0 4px 12px rgba(24,24,27,.08), 0 12px 32px rgba(24,24,27,.08)` | `0 10px 30px rgba(0,0,0,.5)` |

### 의미 색 (콜아웃 전용)

글 본문의 `> [!NOTE]` 등에만 쓴다. 포인트 색과 별개다. 정의는 `global.css` 의 `.markdown-alert-*`.

| 종류 | 라이트 | 다크 |
| --- | --- | --- |
| NOTE | `#1a84ed` | `#5ba8f5` |
| TIP | `#0b9948` | `#3fc97a` |
| IMPORTANT | `#6d24ff` | `#a47dff` |
| WARNING | `#c27c00` | `#f0b43c` |
| CAUTION | `#d90f20` | `#f5525f` |

## 글꼴

| 역할 | 글꼴 | 비고 |
| --- | --- | --- |
| 기본 (`--font-sans`) | **Nunito Variable** → **Pretendard Variable** | Nunito 에 한글이 없어 한글은 Pretendard 로 떨어진다. 둘 다 자체 호스팅(`BaseLayout.astro` 에서 import) |
| 코드 (`--font-mono`) | **JetBrains Mono Variable** | 코드 블록·인라인 코드 |

기본 크기 16px, 줄간격 1.5. 제목 굵기는 **800** 이 기본이고, **900** 은 소개 페이지 인사말에만 쓴다.

### 크기 단계 (실제로 쓰는 값)

| 쓰임 | 크기 / 굵기 | 위치 |
| --- | --- | --- |
| 소개 인사말 | `clamp(28px, 3.4vw, 38px)` / 900 | about |
| 글 제목 | 36px (모바일 28) / 800 | post |
| 헤더 로고(워드마크) | 36px (모바일 28, 푸터 22) / 900 | shell |
| 홈 히어로 이름(워드마크) | 48px (모바일 40) / 900 | shell (ProfileHero) |
| 본문 h2 · h3 · h4 | 26 · 21 · 18px / 800 | post `.prose` |
| 목록 글 제목 | 24px (모바일 20) / 800 | home·category·tag 의 PostRow |
| 글 요약(lede) | 22px (모바일 18) / 400 | post |
| 히어로 제목 | 20px / 800 (`plain` 은 소개 인사말과 같은 값) | shell |
| 본문 | 18px (모바일 17) / 400, 줄간격 1.72 | post `.prose` |
| 일반 글·요약 | 16px | 목록 요약, 메뉴(600) |
| 메타 | 14px / 600 | 작성자·날짜 줄 |
| 태그 | 13–14px / 700, 원래 글자 그대로 | `.tag` |
| 라벨(eyebrow)·카테고리 칩 | 12px / 800, 대문자, 자간 0.08em | `.eyebrow`, `.chip` |

## 레이아웃

- **콘텐츠 폭** `--width-page: 1200px`, **좌우 여백** `--gutter: 16px`.
- **헤더 높이** `--header-h`: 100px (≥1200) · 80px · 60px (≤809). 헤더와, 헤더 뒤로 올라가는 히어로(`Hero snug` — 홈·카테고리·태그)·소개 점 바탕이 같이 쓴다.
- 모든 폭 제한은 공용 클래스 **`.frame`** 으로 한다 (`max-width: 1200 + 16×2`, 가운데 정렬, 좌우 16px).
- 형제 요소 간격은 `gap` 으로 준다.

### 반경

| 토큰 | 값 | 쓰임 |
| --- | --- | --- |
| `--radius-sm` | 8px | 카테고리 칩(`.chip`), 작은 썸네일, 검색 결과 줄 |
| `--radius` | 12px | 이미지, 인용·콜아웃, 코드 블록 |
| `--radius-lg` | 16px | 카드(소개·시리즈·다음 글), 검색창 |
| `999px` | 알약 | 태그, 필터 칩, 아이콘 버튼, 아바타 |

### 화면 폭 기준

| 이름 | 폭 | CSS |
| --- | --- | --- |
| 데스크톱 | 1200px 이상 | (기본) |
| 태블릿 | 810–1199px | `@media (max-width: 1199px)` |
| 모바일 | 809px 이하 | `@media (max-width: 809px)` |

새 기준을 만들지 않는다. 컴포넌트마다 이 두 값만 쓴다.

## 움직임

| 어디 | 무엇 | 값 |
| --- | --- | --- |
| hover 색·면 | 색 전환 | `0.15s` |
| 헤더 메뉴 | 밑줄이 가운데서 펼쳐짐 | `0.25s cubic-bezier(0.44, 0, 0.56, 1)` |
| 히어로 | **GSAP** — 점이 번지고, 사진(구슬)이 튀어오르고, 이름 → 제목 단어(0.07s 간격) → 링크 아이콘이 떠오름 | `Hero.astro`, [`shell.md`](./shell.md#히어로--srccomponentsheroastro) |
| 소개 카드 | **GSAP** — 히어로와 같은 말투. 점 · 카드 → 그림 튀어오름 → 인사 단어 → 소개 → 아이콘 | `about.astro`, [`about.md`](./about.md#들어올-때-움직임) |
| 글 상세 | **GSAP** — 첫 화면은 메타 → 제목 단어 → 요약 · 커버 · 시리즈 → 본문 타임라인, 그 아래 소제목 · 코드 · 표 · 인용 · 그림은 스크롤로 떠오름(ScrollTrigger). 문단은 안 움직인다 | `PostLayout.astro`, [`post.md`](./post.md#움직임--gsap) |
| 목록 이미지 | hover 시 살짝 확대 | `scale(1.03)`, `0.4s` |
| 히어로 점 | 천천히 떠다님, 화면 밖이면 멈춤 | `Particles.astro` 스크립트 (히어로·소개 카드 뒤) |

`prefers-reduced-motion: reduce` 면 **전역에서** 애니메이션·전환을 끈다(`global.css` 끝). GSAP 모션도 돌지 않는다(아래). 파티클은 한 번만 그린다.

### 들어올 때 모션 — GSAP 공용 약속 (`src/lib/motion.ts`)

첫 화면 모션은 전부 **GSAP** 이다(CSS 키프레임은 쓰지 않는다). 한 페이지에서 첫 화면을 맡는 곳은 하나다 — 히어로 · 소개 카드 · 글 머리말.

- **값**: 아래에서 떠오름 `power3.out` 0.8s(`ENTER_DEFAULTS`), 사진·구슬은 튀어오름 `back.out(1.6)` scale 0.85(`POP`), 점은 1.6s 동안 번짐(`DOTS`).
- **번쩍임 방지**: GSAP 은 페이지를 그린 뒤에 뜬다. `BaseHead.astro` 인라인 스크립트가 먼저 `html.gsap-enter` 를 붙이고,
  각 컴포넌트 CSS 가 그걸 보고 **첫 화면 덩어리**(히어로 `.hero > *`, 소개 `.field > *`, 글 `.post-wrap`)를 숨긴다.
  페이지 스크립트는 `enterTimeline()` 으로 클래스를 떼고 **같은 틱에** from 상태를 입힌다 — 그래서 사이에 비치지 않는다.
- **대비책**: 스크립트가 끝내 못 뜨면 2.5초 뒤 클래스를 떼서 그냥 보여준다.
- **끝나면** `clearProps` 로 인라인 `opacity`·`transform` 을 지운다 — 남은 transform 이 고정 위치 요소의 기준을 바꾸지 않게.
- **모션 줄이기**면 클래스를 붙이지 않고, `enterTimeline()` 이 `null` 을 돌려줘 아무것도 하지 않는다.
- 새 페이지에 첫 화면 모션을 넣으려면: 첫 화면 덩어리에 `:global(html.gsap-enter) … { opacity: 0 }` 를 걸고, 스크립트에서 `enterTimeline()` 을 받아 `from` 을 건다.

## 테마 (라이트/다크)

`BaseHead.astro` 의 인라인 스크립트가 첫 페인트 전에 정한다.

1. OS 가 다크를 선호하면 **항상 다크**.
2. 아니면 **한국 시각** 06–18시 라이트, 그 밖 다크.
3. 헤더 토글로 고른 값은 **다음 자동 전환 시점까지만** 유지된다 (`localStorage` 의 `theme-override`).

모바일 주소창 색(`meta[name="theme-color"]`)도 `--bg` 값으로 맞춘다.

## 공용 클래스 (`global.css`)

| 클래스 | 모양 |
| --- | --- |
| `.frame` | 1200px 폭 프레임 |
| `.divider` | 콘텐츠 폭 안의 1px 선 (`--border`, 투명도 0.6) |
| `.eyebrow` | 12px/800 대문자 라벨 ("ALL ARTICLES") |
| `.chip` | 회색 사각 칩, 대문자 (카테고리) |
| `.tag` | 회색 알약, 원래 글자 (태그) |
| `.prose` | 글 본문 타이포 (`post.md` 참고) |
| `.sr-only` | 화면엔 안 보이고 스크린리더만 읽음 |

## 규칙

- 🔴 **색은 토큰으로만 쓴다.** 새 hex 를 컴포넌트에 직접 쓰지 않는다. 예외는 넷뿐이고, 모두 위 단계표 안의 값이다:
  콜아웃 의미 색, 히어로 점 `--dot-*`, 404 구슬 `--orb-*`, SNS 카드 템플릿(`scripts/og-card.html`, 브라우저 밖에서 렌더링돼 토큰을 못 읽는다).
- 🔴 **포인트 색은 에버그린 하나.** 두 번째 강조색을 들이지 않는다.
- 🔴 **다크 값을 같이 정한다.** 라이트만 정의한 색은 다크에서 깨진다. `html.dark` 블록에 짝을 둔다.
- 면은 그림자 대신 `--border` 선과 여백으로 나눈다.
- 사이트 색을 바꾸면 `scripts/og-card.html` 의 팔레트도 맞춘다(CLAUDE.md "SNS 공유 카드").

# post — 글 상세

## 한눈에

| | |
| --- | --- |
| URL | `/[lang]/blog/<slug>/` (slug = 파일명, CLAUDE.md "새 글 쓸 때 규칙") |
| 소스 | 경로 `src/pages/[lang]/blog/[...slug].astro` → 화면 `src/layouts/PostLayout.astro` |
| 컴포넌트 | `PostMeta`, `ProfileWidget`, `SocialLinks`, `BackToTop`, `Comments` + 본문 스타일 `global.css .prose` |
| 헤더 현재 메뉴 | 홈 |
| 검색 노출 | ✓ (canonical 은 self — CLAUDE.md) |
| 페이지 제목 | "<글 제목> · Jieun" / 설명 = frontmatter `description` / 공유 이미지 = [`og-card.md`](./og-card.md) |

## 이 페이지가 하는 일

블로그에서 가장 오래 머무는 화면. **긴 기술 글을 끝까지 읽게** 하는 게 목표다.
읽는 위치(진행바·목차), 글 사이 이동(시리즈·다음 글), 쓴 사람(사이드바 프로필)을 옆에서 조용히 받쳐준다.
채용 담당자가 링크드인에서 글 하나로 바로 들어오는 경우가 많아, **사이드바 프로필이 첫 인상** 역할도 한다.

## 화면 구조

```plaintext
데스크톱 (≥1200)
▔▔▔▔▔▔▔▔▔▔ 읽기 진행바 3px (--accent-bg, 화면 맨 위 고정) ▔▔▔▔▔▔▔▔▔▔
┌ Header ─────────────────────────────────────────────────────────────┐
│                                                           위아래 64px │
│ ┌ 본문 (나머지 폭) ───────────────────────┐ ┆ ┌ 사이드바 240 ─────┐ │
│ │ 🙂 Jieun ╱ 날짜 · 8분 분량 · 수정 날짜  [SYSTEMS]│ ┆ │ 목차        3 / 7 │ │
│ │                                          │ ┆ │ ┃ 1 주제어         │ │
│ │ 글 제목 36/800                            │ ┆ │ ┃   부제           │ │
│ │ 요약(lede) 22 --text-muted                │ ┆ │ ┃   · h3 제목      │ │
│ │ [이 글은 아직 한국어 번역이 없습니다.]      │ ┆ │ │ 2 주제어         │ │
│ │ ┌ 커버 ──────────────────────────────┐   │ ┆ │ │ 3 주제어         │ │
│ │ └──────────────────────────────────┘   │ ┆ │                   │ │
│ │   캡션 14 가운데                          │ ┆ │ 🙂 [글 20개]       │ │
│ │ ┌ SERIES · 메신저 부하 테스트 ─────────┐   │ ┆ │ Jieun             │ │
│ │ │ 1. …   2. (지금 글, 굵게)   3. …     │   │ ┆ │ Deserve what …    │ │
│ │ └──────────────────────────────────┘   │ ┆ │ ⌂ in ✉            │ │
│ │                                          │ ┆ └───────────────────┘ │
│ │ 본문 18/1.72 …                            │ ┆   ↑ 위에 붙어 따라옴   │
│ │                                          │ ┆                       │
│ │ #tag  #tag  #tag                         │ ┆                       │
│ │ NEXT ARTICLE                             │ ┆                       │
│ │ ┌[이미지 118]  다음 글 제목 18/800 ─────┐ │ ┆                       │
│ │ ───────────────────────────────────────  │ ┆                       │
│ │ 댓글                                      │ ┆                       │
│ │ ┌ giscus (GitHub Discussions) ───────┐    │ ┆                       │
│ │ │ 댓글 N개 · 로그인해서 댓글 남기기    │    │ ┆                       │
│ │ └──────────────────────────────────┘    │ ┆                       │
│ └──────────────────────────────────────────┘ ┆                       │
│                    ┆ = 1px 세로선 (위아래로 흐려지는 그라데이션), 간격 48 │
└ Footer ─────────────────────────────────────────────────────────────┘

태블릿·모바일 (≤1199) — 한 줄로 쌓인다
메타 · 제목 · 요약 · 커버 · 시리즈
┌ ▸ 목차 (접힘, <details>) ──────────────┐   ← 사이드바 목차 대신
│ │ 1  문제                              │   펼치면 챕터가 전부 보인다
│ │    배포 시각과 장애 시각이 정확히 겹친다 │   ← 부제 13px --text-subtle
│ │    · 죽은 건 앱이 아니라 리버스 프록시였다│   ← h3 13px
│ │ 2  원인                              │
│ │    …                                 │
└──────────────────────────────────────┘
본문 · 태그 · 다음 글 · 댓글
프로필 위젯 (최대 360px)
```

## 구성 요소

### 읽기 진행바

화면 맨 위에 고정된 3px 막대(`z-index: 60`, 헤더보다 위). 스크롤한 비율만큼 `--accent-bg` 로 찬다.

### 머리말 — `.post-header`

| 요소 | 모양 |
| --- | --- |
| 메타 줄 | 왼쪽 `PostMeta`(아바타 · 이름 ╱ 날짜 · N분 분량 · 수정 날짜), 오른쪽 카테고리 `.chip`. 좁으면 줄바꿈 |
| 제목 `<h1>` | 36px/800, 줄간격 1.2, 위 20px |
| 요약 `.lede` | 22px/400, 줄간격 1.6, `--text-muted`, 위 16px. frontmatter `description` |
| 번역 없음 안내 | 다른 언어판이 없을 때만. `--bg-2` 면 + `--border` 선 + `--radius` 상자, 14px. **문구는 다른 언어로** 쓴다 (한국어 글이면 영어 문장 — 그 언어 독자에게 하는 말이라서) |

- 읽는 시간: 한글 분당 500자 + 영어 분당 200단어로 셈 (`readingMinutes`, 코드 블록 제외).
- 수정 날짜는 frontmatter `updatedDate` 가 있을 때만.

### 커버 — 선택

frontmatter `cover` 가 있을 때만. 폭 100%, `--radius` + `--hairline`, 위 40px. 누르면 확대([`shell.md`](./shell.md#이미지-확대--baselayoutastro--globalcss-lightbox)).
`coverCaption` 이 있으면 아래 14px `--text-subtle` 가운데 정렬.

### 시리즈 상자 — 선택

frontmatter `series` 가 있을 때만. 위 32px.

- `--bg-2` 면 + `--border` 선 + `--radius-lg`, 안쪽 16/20px.
- 라벨: `.eyebrow` 를 **초록**으로 — "SERIES · <seriesTitle>".
- 편 목록은 번호 목록(15px). 다른 편은 링크(`--text-muted`/600, hover 초록), **지금 편은 링크 없이 `--text`/800**.
- 편 순서는 `seriesOrder`.

### 본문 — `.prose` (`global.css`)

| 요소 | 모양 |
| --- | --- |
| 문단 | 18px (모바일 17), 줄간격 1.72, `--text-2`, 문단 간격 20px |
| h2 · h3 · h4 | 26 · 21 · 18px / 800, 위 여백 48 · 36 · 28 |
| 제목 앵커 | h2·h3 에 마우스를 올리면 끝에 `#` 가 나타난다. 헤더에 가리지 않게 `scroll-margin-top: 96px` |
| 굵게 | 800, `--text` |
| 링크 | 700, **2px 옅은 초록 밑줄**(`--accent-soft`) → hover 시 진한 초록 |
| 목록 | 들여쓰기 1.4em, 항목 간격 8px, 마커 `--text-faint` |
| 인용 `>` | **왼쪽 막대가 아니라 상자**: `--bg-2` 면 + `--border` 선 + `--radius`, 안쪽 20px, 20px/700 |
| 콜아웃 `> [!NOTE]` 등 | 인용 상자와 같은 모양에 **의미 색**만 입힌다 — 테두리 22%, 면 6%, 제목 12px/800 대문자. 색은 [`foundations.md`](./foundations.md#의미-색-콜아웃-전용) |
| 인라인 코드 | JetBrains Mono 0.84em, `--code-bg`, 반경 6px |
| 코드 블록 | Expressive Code. 테마 `github-light` / `github-dark`(`.dark`), 반경 12px, 테두리는 `0 0 0 1px var(--border)`, 14px. **줄바꿈 켬, 줄번호 끔**(블록마다 `showLineNumbers` 로 켤 수 있음) |
| 표 | 15px. 좁으면 **표만** 가로 스크롤. 머리 `--surface`, 짝수 줄 `--bg-2`, 칸 선 `--border` |
| 이미지 | `--radius` + `--hairline`, 누르면 확대. **문단에 이미지만 있으면** 자동으로 `<figure>` 가 되고 alt 가 캡션(14px, 가운데)이 된다 |
| 가로줄 `---` | `--border` 1px, 위아래 40px |

### 태그

본문 아래 48px. `.tag` 알약들 — `#tag`, 누르면 [태그별 글](./tag.md).

### 다음 글 — `.up-next`

본문 아래 56px. 라벨 `.eyebrow` + 카드 하나.

- **"다음 글" = 이어서 읽을 더 오래된 글.** 가장 오래된 글이면 대신 더 최신 글을 "최신 글 / Newer Article" 로 건다.
- 카드: `--bg-2` 면 + `--border` + `--radius-lg`, 안쪽 14px, hover `--surface`. 왼쪽 커버 118px(비율 1.5, `--radius-sm`, 커버 없으면 생략) + 제목 18px/800.

### 댓글 — `.post-comments` (`src/components/Comments.astro`)

다음 글 아래 56px, 위에 `--border` 구분선 + 40px 여백. 라벨 `.eyebrow`("댓글 / Comments") 아래 **giscus** 창.

- **giscus = GitHub Discussions 로 굴러가는 댓글.** 방문자는 GitHub 로그인만 하면 남길 수 있고, 답글·이모지 리액션도 된다. 별도 서버·DB 가 없다.
- 설정은 `Comments.astro` 위쪽 `giscus` 객체 한 곳에 모아 두었다.

  | 값 | 지금 | 뜻 |
  | --- | --- | --- |
  | `repo` · `repo-id` | `jieun9999/jieun9999.github.io` · `R_kgDOTON5cA` | 댓글이 쌓이는 레포 |
  | `category` · `category-id` | `Announcements` · `DIC_kwDOTON5cM4DGEZB` | 글마다 디스커션 하나. 관리자만 새로 못 열게 잠긴 카테고리라 스팸이 덜하다 |
  | `mapping` · `strict` | `pathname` · `1` | **URL 경로로 스레드를 잡는다** → `/en/` 과 `/ko/` 판은 댓글이 따로 쌓인다. `strict` 는 비슷한 제목에 잘못 붙는 걸 막는다 |
  | `lang` | 글 언어 | 댓글창 UI 언어도 글을 따라간다 |
  | `loading` | `lazy` | 화면에 들어올 때 뜬다. 자리는 `min-height: 200px` 로 미리 잡아 스크롤이 튀지 않게 |

- 창이 뜨기 전에 현재 테마를 읽어 `data-theme` 으로 넘기고, 그 뒤 `html.dark` 변화를 `MutationObserver` 로 지켜보다 `postMessage` 로 넘긴다 (헤더 토글·시각에 따른 자동 전환 둘 다).

### 사이드바 — 데스크톱만 옆에

- 폭 240px, 화면 위 30px 에 붙어 따라온다. 내용이 길면 사이드바 안에서 스크롤(막대는 숨김). 항목 간격 48px.
- 본문과 사이에 **1px 세로선** — 위아래 끝이 흐려지는 그라데이션(`--bg-2 → --surface-2 → --bg-2`).

#### 목차 — 제목(h2·h3)이 2개 이상일 때만

```plaintext
목차                 3 / 7      ← 지금 챕터 / 전체 (숫자 고정폭)
│ 1  문제                        ← 읽은 챕터: 옅은 초록 레일, --text-subtle
│ 2  원인
┃ 3  해결                        ← 지금 챕터: 초록 2px 레일 + 초록 800
┃    부제 문장 13px              ← 지금 챕터만 펼침
┃    · h3 제목                   ← 지금 읽는 h3 는 굵게
│ 4  결과
```

- **h2 = 챕터 한 줄**, h3 는 그 아래로 묶여 **지금 읽는 챕터만** 펼친다.
- h2 를 `1. 주제 — 부제` 형식으로 쓰면 번호·주제어·부제를 떼어 보여준다. 주제어는 두 줄에서 자른다.
- 번호가 없는 글은 순서로 센다 (`3 / 7`).
- 스크롤 위치를 따라 자동으로 바뀐다 (화면 위 10–30% 구간에 들어온 제목 기준).

#### 접는 목차 — 태블릿·모바일 (≤1199)

사이드바 대신 시리즈 상자 아래에 `<details>` 로 들어간다 (테두리 + `--radius-lg`, 안쪽 14/18px, 처음엔 접힘).

- **사이드바와 같은 챕터 묶음**을 쓴다: 번호 · 주제어 (`--text`, hover 초록) → 부제 13px `--text-subtle` → h3 `· 제목` 13px.
- 글 위에 있어 읽는 위치를 따라가지 않으니 **"지금 챕터"가 없다.** 그래서 모든 챕터를 펼친 채로 보여준다.
- 부제·h3 는 주제어와 줄이 맞게 38px 들여쓴다 (번호 없는 글은 12px). 왼쪽 레일 1px `--border`.

#### 프로필 위젯 — `src/components/ProfileWidget.astro`

32px 아바타 + 글 수 알약("글 20개" / "20 posts") → 이름 16px/800 → 모토 14px `--text-muted` → 링크 아이콘(복사 알림은 아래 왼쪽).

## 움직임 — GSAP

첫 화면 타임라인은 히어로 · 소개 카드와 같은 공용 약속(`src/lib/motion.ts`)을 쓰고, **ScrollTrigger 로 스크롤 모션까지 하는 건 이 페이지뿐**이다. 스크립트는 `PostLayout.astro` 맨 아래.
결은 홈 히어로와 같은 "아래에서 떠오름"이되 조금 더 크게(y 28px) 움직인다. 이징 `power3.out`, 길이 0.8s.

### 1. 첫 화면 — 열 때 한 번 (타임라인)

| 차례 | 무엇 | 값 |
| --- | --- | --- |
| 1 | 메타 줄 | y 12, 0s |
| 2 | 제목 단어가 하나씩 | y 28, 0.1s 부터 단어마다 0.06s. **단어가 많으면 간격을 줄여 0.45s 안에 다 출발** (긴 제목이 끌지 않게) |
| 3 | 요약 → 번역 안내 → 커버 → 시리즈 → 접는 목차 → 본문 | y 28, 0.1s 간격, 제목이 끝나기 0.65s 전부터. **있고 보이는 것만** 차례를 받는다 |
| – | 사이드바(≥1200) 목차 머리 → 챕터 → 프로필 | y 16, 0.04s 간격, 3 과 같이 출발 |

- 번쩍임 방지 · 대비책 · `clearProps` 는 공용 약속을 따른다([`foundations.md`](./foundations.md#들어올-때-모션--gsap-공용-약속-srclibmotionts)).
  뜨기 전엔 `.post-wrap` 전체를 숨겨 둔다.

### 2. 그 아래 — 화면에 들어올 때 (ScrollTrigger.batch)

- 대상: 본문의 **소제목(h2·h3) · 코드 블록 · 표 · 인용 · 콜아웃 · 그림**, 태그, 다음 글, 댓글. 좁은 화면(≤1199)에선 맨 아래로 내려간 사이드바(프로필)도.
- **문단은 움직이지 않는다.** 긴 글을 읽는 흐름을 끊지 않게.
- 화면 아래 92% 선에 닿으면 y 32 → 0 으로 떠오른다. 한 번만.
- **처음에 화면 아래 있던 것만 숨긴다.** 이미 보이는 걸 숨겼다 띄우면 깜빡인다.
- 빠르게 내리거나 목차로 건너뛰어 여러 개가 한꺼번에 들어오면: 이미 지나친 건 바로 보이고, 화면 안 것만 0.4s 안에 다 출발하게 간격을 줄인다.
- `#앵커` 로 열리거나 새로고침으로 위치가 복원돼 "들어오는" 순간 없이 지나쳐 있으면, ScrollTrigger 가 위치를 다시 잴 때(`onRefresh`) 띄운다.

### 끄는 경우

- `prefers-reduced-motion: reduce` 면 첫 화면 모션도 스크롤 모션도 돌지 않는다 — 처음부터 다 보인다.
- 인쇄할 때는 아직 안 띄운 블록도 보이게 `@media print` 로 되돌린다.

## 반응형

| | 데스크톱 | 태블릿 (≤1199) | 모바일 (≤809) |
| --- | --- | --- | --- |
| 배치 | 본문 + 세로선 + 사이드바 | 한 줄로 쌓음, 세로선 없음 | 같음 |
| 위아래 여백 | 64 | 36 | 24 |
| 목차 | 사이드바 (지금 챕터만 펼침) | 시리즈 아래 **접는 상자** (챕터 전부 펼침) | 같음 |
| 프로필 위젯 | 사이드바 | 본문 아래, 최대 360px | 같음 |
| 제목 / 요약 / 본문 | 36 / 22 / 18 | 같음 | 28 / 18 / 17 |
| 커버 위 여백 | 40 | 40 | 28 |
| 다음 글 이미지 / 제목 | 118 / 18 | 같음 | 88 / 16 |

## 다크 모드

- 코드 블록이 `github-dark` 로 바뀐다 (`html.dark` 기준).
- 콜아웃 의미 색이 밝은 쪽으로 바뀐다.
- 진행바·지금 챕터 레일은 밝은 초록.
- 댓글창(giscus)은 라이트에서 `light`, 다크에서 **`transparent_dark`** 를 쓴다 — 사이트 다크 배경이 giscus 기본 다크보다 어두워서, 배경이 비치는 테마라야 면이 이어진다.

## 한/영 차이

- 번역이 있으면 헤더 언어 버튼이 **그 번역 글**로, 없으면 다른 언어 홈으로 간다. `hreflang` 은 실제 있는 번역만 건다.
- 라벨: "On this page / 목차", "Series / 시리즈", "Next Article / 다음 글", "Newer Article / 최신 글", "min read / 분 분량", "Updated / 수정", "Comments / 댓글".
- 날짜 형식은 언어별.

## 규칙

- 🔴 **글쓰기 규칙은 CLAUDE.md 를 따른다** — 한 줄에 물결표 두 개 금지, canonical 은 건드리지 않음, 새 글 뒤 `npm run og`, 전 회사 자산을 "우리 X"로 부르지 않음.
- 🔴 인용은 **상자** 모양이다. 왼쪽 굵은 막대 인용으로 바꾸지 않는다 (콜아웃이 같은 상자 모양을 공유한다).
- 🔴 코드 블록 모양은 `astro.config.mjs` 의 `expressiveCode` 설정에서만 바꾼다. `.prose` CSS 로 덮지 않는다.
- 목차가 보기 좋으려면 h2 를 `1. 주제 — 부제` (앞뒤 공백 있는 em dash) 로 쓴다.
- 캡션을 달고 싶은 이미지는 **문단에 이미지만** 두고 alt 에 캡션 문장을 쓴다.
- 새 본문 요소를 만들면 `.prose` 에 넣고 이 문서의 본문 표에 한 줄 추가한다.
- 🔴 **글 URL 을 바꾸면 그 글에 달린 댓글이 끊긴다.** `mapping=pathname` 이라 경로가 곧 스레드 열쇠다. 옮겨야 하면 해당 디스커션 제목을 새 경로로 고친다.
- 🔴 댓글 레포·카테고리를 옮기면 `Comments.astro` 의 네 값(`repo`·`repo-id`·`category`·`category-id`)을 **같이** 바꾸고 이 문서 표도 고친다. 옛 댓글은 따라오지 않는다.

## 알아둘 것

- 사이드바 목차는 챕터 묶기·읽은 챕터 표시·위치 숫자까지 붙인 버전이다(`0f86477`). 예전엔 h2·h3 를 평평하게 나열했다.
- 태블릿·모바일의 접는 목차는 처음엔 h2·h3 를 평평하게 나열했다(h3 만 들여쓰기). 사이드바와 같은 `chapters` 데이터로 챕터별로 묶었다 (2026-09-19). 스크롤 추적 스크립트는 `.toc .chapter` 만 보므로 접는 목차에는 걸리지 않는다.
- 댓글은 올리브영 기술블로그처럼 GitHub 로그인 방식으로 붙였다 (2026-09-21). 거기 쓰인 **utterances**(Issues 기반) 대신 후속판인 **giscus**(Discussions 기반)를 골랐다 — utterances 는 2022 년 이후 사실상 관리가 멈췄고, 답글 스레드·한국어 UI·테마 전환이 없다. Discussions 를 쓰면 블로그 코드 이슈와 댓글이 탭으로 갈린다.
- giscus 창 안쪽은 GitHub 기본 생김새다(글꼴·초록 포인트색이 사이트와 다르다). 맞추려면 giscus 커스텀 테마 CSS 를 `public/` 에 올리고 `data-theme` 을 그 URL 로 주면 된다 — 지금은 안 했다.
